git init //initialize a reprository
git status
git add .
git log --oneline
git log --all
git log

git commit -m "message" //commit request

git branch -a //current branch
git checkout comitID //remote branch
git checkout abc //new branch call abc

git push URL BRANCH_NAME //to push the code
git remote add origin URL //to save to url
git pull origin master //pull request to master
rm -rf .git //remove git from project

snap remove intellij-idea-community // uninstall intelij ide

//Udemy
git reset HEAD README.md // reset add or Backing out changes
git checkout -- README.md // Clean working directory
git mv a.txt b.txt // Rename file using git
git rm b.txt // Delete file
vi .gitignote // after enter file name that is not part of your git 
git diff //compare HEAD to resent changes
git diff commitID HEAD //compare deferance two commit
git branch //To see all branch
git checkout -b updates //create a branch name updates
